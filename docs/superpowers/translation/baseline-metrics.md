# Baseline metrics (BASE_RU)

BASE_RU: `5148d66afea0c857e1909797f4a286c36576c529`

Зафиксировано 2026-08-09, до первого переводческого коммита. Команды и вывод — Task 1 брифа, Step 1. Слой 1 верификации (после перевода) сравнивает эти числа с EN-корпусом; отклонения допустимы только по задокументированной карте замен (`example-replacement-map.md`).

## 1. Счетчики корпуса

```
$ grep -c '^### [0-9]*\.' SKILL.md
24
```
Master rules в SKILL.md: **24**.

```
$ sed -n '/^## Quick reference/,/^Полные правила/p' SKILL.md | grep -c '^[0-9]*\.'
24
```
Пунктов в Quick reference: **24**. Совпадает с числом master rules (ожидаемо 1:1).

```
$ grep -c '^### [0-9]' reference/full-rules.md
51
```
Разделов в full-rules.md (### N.M): **51**. Разбивка по группам: 1.1-1.3 (3), 2.1-2.7 (7), 3.1-3.5 (5), 4.1-4.20 (20), 5.1-5.6 (6), 6.1-6.3 (3), 7.1-7.7 (7) = 51.

```
$ sed -n '/^## 8\./,$p' reference/full-rules.md | grep -c '^[0-9]*\. '
40
```
Итоговых принципов одной строкой (раздел 8): **40**. Совпадает с ожиданием брифа.

```
$ grep -c '^- \[ \]' checklists/self-check.md
63
```
Пунктов self-check: **63**. Совпадает с ожиданием брифа.

```
$ grep -c '^- \[ \]' reference/full-rules.md
26
```
Пунктов чеклиста §6.2 внутри full-rules.md: **26**. Совпадает с ожиданием брифа.

```
$ for f in SKILL.md reference/*.md templates/*.md checklists/*.md; do echo "$f: $(grep -c '<example>' $f)"; done
SKILL.md: 10
reference/full-rules.md: 8
reference/modal-registers.md: 0
templates/agentic-task.md: 2
templates/character-frame.md: 6
templates/extraction-prompt.md: 5
templates/identification-frame.md: 6
templates/one-shot-task.md: 6
checklists/input-triage.md: 0
checklists/self-check.md: 1
```
`<example>`-блоков по файлам: SKILL.md 10, full-rules.md 8, modal-registers.md 0 (регистры демонстрируются через инлайн-блоки кода, не через `<example>`-теги —架构особенность, не пропуск), templates суммарно 25, checklists суммарно 1. Итого по корпусу: **40** блоков `<example>`.

```
$ grep -rn '[Ёё]' SKILL.md reference/ templates/ checklists/ README.md CLAUDE.md
```
```
SKILL.md:211
reference/modal-registers.md:21,65,67,103,105,106,177,179,206,208
reference/full-rules.md:212,842,847,859,866,871
templates/identification-frame.md:27
CLAUDE.md:11
```
19 строк с вхождением `[Ёё]` (`grep -c` = 19), 20 вхождений буквы всего (`grep -o '[Ёё]' ... | wc -l` = 20 — одна строка, CLAUDE.md:11, содержит букву дважды: строчную ё и прописную Ё). Полный построчный список с текстом — в `example-replacement-map.md`, раздел 1 (карта замен на Ё → em dash ban). Все вхождения зафиксированы на BASE_RU.

Список политес-формул (для карты замен, раздел 2):
```
$ grep -rn 'С уважением\|Здравствуйте\|Благодарю за обращение' SKILL.md reference/ templates/ checklists/
SKILL.md:201
reference/full-rules.md:132
checklists/self-check.md:59
```
3 вхождения на BASE_RU (не 4, как в спеке — `identification-frame.md` этот список не содержит; проверено отдельно, см. `example-replacement-map.md` раздел 2 для деталей).

Title-case пример (правило 18 / full-rules 4.13, для карты замен раздел 3):
```
$ grep -n 'title case\|Title Case' SKILL.md reference/full-rules.md
SKILL.md:318
reference/full-rules.md:459
```
2 вхождения на BASE_RU.

## 2. Регистровая карта — master rules SKILL.md (правила 1-24)

Маркеры искались по regex: `НИКОГДА`, `ВСЕГДА` (капс), `\bникогда\b`, `\bвсегда\b`, `\bдолжн[аоы]?\b`, `\bможет\b|\bможно\b`, `\bизбега(ет|ть|ют)\b`, `\bпредпоч(итает|тительн)\w*\b`, `\bNEVER\b`, `\bALWAYS\b`, `\bshould\b`, `\bcan\b`, `\bavoids\b`, `\bprefers\b` — внутри текста каждого правила (от заголовка `### N.` до следующего заголовка).

- правило 1: (нет маркеров)
- правило 2: NEVER, ALWAYS, should
- правило 3: (нет маркеров)
- правило 4: (нет маркеров)
- правило 5: (нет маркеров)
- правило 6: (нет маркеров)
- правило 7: (нет маркеров)
- правило 8: (нет маркеров)
- правило 9: (нет маркеров)
- правило 10: (нет маркеров)
- правило 11: (нет маркеров)
- правило 12: всегда, должен
- правило 13: (нет маркеров)
- правило 14: (нет маркеров)
- правило 15: может
- правило 16: (нет маркеров)
- правило 17: может
- правило 18: должен, может
- правило 19: (нет маркеров) — заголовок правила содержит слово «никогда», но оно вне тела текста (не захвачено разбивкой по секциям); см. примечание ниже
- правило 20: может
- правило 21: (нет маркеров)
- правило 22: (нет маркеров)
- правило 23: может
- правило 24: никогда

Примечание к правилу 19: заголовок «Секреты никогда не попадают в промпт» содержит «никогда» как часть названия правила, а не как модальный маркер внутри тела. При переводе заголовок дает EN «Secrets never enter the prompt» — маркер `never` появляется в EN-заголовке, что нужно учесть слоем 1 (регистр не должен «потеряться» из-за того что в RU он был в заголовке, а не в теле).

## 3. Регистровая карта — full-rules.md (разделы 1.1-7.7)

- 1.1: (нет)
- 1.2: (нет)
- 1.3: (нет)
- 2.1: НИКОГДА, всегда, должен, может, предпочитает, NEVER, should, can, avoids
- 2.2: NEVER, should
- 2.3: должен
- 2.4: (нет)
- 2.5: (нет)
- 2.6: никогда
- 2.7: NEVER, ALWAYS, should
- 3.1: может
- 3.2: (нет)
- 3.3: всегда
- 3.4: (нет)
- 3.5: (нет)
- 4.1: должен
- 4.2: всегда
- 4.3: (нет)
- 4.4: (нет)
- 4.5: (нет)
- 4.6: может
- 4.7: (нет)
- 4.8: должен
- 4.9: (нет)
- 4.10: никогда, должен, может, can
- 4.11: (нет)
- 4.12: может
- 4.13: должен, может
- 4.14: (нет)
- 4.15: (нет)
- 4.16: (нет)
- 4.17: может
- 4.18: может
- 4.19: (нет)
- 4.20: может
- 5.1: (нет)
- 5.2: может, предпочитает
- 5.3: (нет)
- 5.4: никогда, должен
- 5.5: никогда, всегда
- 5.6: (нет)
- 6.1: всегда, может
- 6.2: должен, может, should, can
- 6.3: НИКОГДА, ВСЕГДА, никогда, всегда, избегает, should, can, avoids
- 7.1: (нет)
- 7.2: всегда, должен
- 7.3: (нет)
- 7.4: (нет)
- 7.5: (нет)
- 7.6: (нет)
- 7.7: никогда, должен

Раздел 2.1 и раздел 6.3 (контр-примеры) — самые насыщенные маркерами: 2.1 потому что перечисляет все шесть регистров с примерами каждого (ожидаемо), 6.3 потому что содержит демонстрационные ❌/✅ пары, использующие полный спектр маркеров намеренно. Для этих двух разделов слой 1 EN-версии должен проверяться особенно внимательно — сдвиг любого из перечисленных маркеров здесь искажает сам объясняемый механизм, а не только пример.

## 4. Как использовать эту карту

Слою 1 верификации (после перевода каждого файла и одним проходом по всему корпусу) нужно повторить те же команды над EN-корпусом и сверить: (а) все числовые счетчики раздела 1 совпадают, кроме документированных замен Ё → em dash / title case → sentence case; (б) для каждого правила и раздела набор маркеров из разделов 2-3 присутствует в EN-версии в том же регистре (никогда → never/NEVER, а не смягчилось до should/avoids, и наоборот).
